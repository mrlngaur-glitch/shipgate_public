# `dashboard/`

Placeholder, scaffolded per the report's fixed directory layout at Phase 0. No code lands
here until a dashboard surface is actually scheduled.

**Found live, Gate C (2026-08-17): this directory existed only on the analyst's
local disk, never committed** — git cannot track an empty directory, so a genuinely fresh
clone silently lost it, and `tests/unit/test_scaffold.py::test_repo_layout_matches_report_section_13_1`
failed on that clone (386/387) despite passing in every local run since Phase 0 (at the
time, a GitHub remote existed and three real pushes had triggered CI, but every one had
failed to start — zero jobs, no logs, account-level and unrelated to this repository; CI
has since run for the first time, see `SECURITY.md` for the current, re-confirmed status
of every claim this repository makes about itself). This file exists so the directory
survives a real clone; delete it once real content lands here.
