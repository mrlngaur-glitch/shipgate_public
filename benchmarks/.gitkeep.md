# `benchmarks/`

Placeholder, scaffolded per the report's fixed directory layout at Phase 0. Task 3.6
(a latency benchmark harness in CI, <10ms p99 added overhead, published) is
not built yet — confirmed by inspection during Gate C (2026-08-17): this
directory is genuinely empty, no benchmark code exists anywhere in the repo. Gate C's own
"Benchmark green" done-condition could not be measured for this reason and was reported as an
honest gap, not a fabricated pass.

**Also found live, same session: this directory existed only on the analyst's local disk,
never committed** — git cannot track an empty directory, so a genuinely fresh clone silently
lost it, and `tests/unit/test_scaffold.py::test_repo_layout_matches_report_section_13_1` failed
on that clone (386/387) despite passing in every local run since Phase 0 (at the time, a
GitHub remote existed and three real pushes had triggered CI, but every one had failed to
start — zero jobs, no logs, account-level and unrelated to this repository; CI has since
run for the first time, see `SECURITY.md` for the current, re-confirmed status of every
claim this repository makes about itself). This file exists so the directory survives a
real clone; delete it once task 3.6's real content lands here.
